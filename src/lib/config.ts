import { dev } from "$app/environment"

export const title = "Baptiste's portfolio"
export const description = "The portfolio of Baptiste Leroyer made in SVeltekit."
export const url = dev ? "http://localhost:5173" : "https://baptiste.zip"
